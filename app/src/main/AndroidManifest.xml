<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android 12+ -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Kuni Android 11: skaneerimine nõudis asukohaluba -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Kasutusel ainult siis, kui Bluetoothi luba puudub: connectedDevice
         tüüpi teenust ei tohi siis käivitada, aga demo sond, alarm ja ajalugu
         peavad ka ilma Bluetoothita töötama. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- specialUse tüüpi ei ole enne Android 14 olemas. Android 10–13 vajab
         loata olukorras seega dataSync'i — tüübita teenust ei luba juba
         Android 10. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Ilma selleta võib Android pika küpsetuse ajal teenuse maha võtta.
         Play Store piirab seda luba, aga see äpp on isiklikuks kasutuseks. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.A5S">

        <!-- Compose tuleb konfiguratsioonimuutustega ise toime, seega ei ole
             Activity taasloomist vaja. Ilma selleta kaotas ekraani pööramine
             navigatsiooni ja lähtestas demo sondi küpsetuse. -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|density|fontScale|uiMode|layoutDirection|locale"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.A5S">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ble.ThermometerService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|specialUse|dataSync">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Demo probe simulation and cook alarm when the user has
                    declined Bluetooth permission and no real thermometer is connected." />
        </service>

    </application>

</manifest>
